# Blockers

| Item | Why it matters | Who provides it | Status |
|---|---|---|---|
| `GHL_PIT` is rejected by the LeadConnector API — every `execute_operation` call (tried `search-users`, `get-pipelines`, and again on 2026-08-26 with `search-users`) returns 401 (`E01 - Unauthorized request` / `Invalid Private Integration token`). `search_operations` itself works (it's a local catalog lookup, not an upstream call), so it does NOT confirm the token — only `execute_operation` proves that. Retested again 2026-08-26: still failing, same 401 on `search-users`. | Blocks the entire audit — no data can be pulled at all, not just the location lookup, until this is fixed | Ashley — regenerate or verify the Private Integration Token in HighLevel (Settings > Private Integrations) and update `.env` | Open |
| Location ID for the sub-account being audited | Needed in `audits/context.md` to confirm every pull is scoped to the right client before the audit starts | Ashley — HighLevel Settings > Business Profile (blocked until the PIT above is fixed, since `list_locations` also fails with a 500 "dependencies are not configured" error and `get-location` needs a locationId we can't yet confirm) | Open |
